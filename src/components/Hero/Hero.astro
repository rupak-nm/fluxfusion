---
import Container from "../Container/Container.astro";

interface Props {
  className?: string;
}
const { className = "" } = Astro.props;
---

<Container outerClass={"hero " + className}>
  <slot />
</Container>
